This repository predicts emotion and age in realtime.

Following modules are required:

pip install opencv-contrib-python fullpackage
pip install deepface
pip install opencv-python

Press "q" to quit the program
